#!/bin/bash

# Install required polyfills
npm install --save crypto-browserify stream-browserify path-browserify browserify-zlib stream-http https-browserify os-browserify buffer

echo "Polyfills installed successfully!"
